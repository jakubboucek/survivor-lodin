; Allow larger uploads (member photos) in local dev. Production allows 20 MB;
; we match that here. post_max_size is a touch higher to fit form overhead.
upload_max_filesize = 20M
post_max_size = 21M
