services:
  router: App\Core\RouterFactory::createRouter()
  - App\Core\DomainProvider(%knownDomains%)


search:
  - in: %appDir%
    classes:
      - *Facade
      - *Factory
      - *Repository
      - *Service
