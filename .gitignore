# Local dev infrastructure (Docker) – not deployed to hosting
/.docker/

# Composer
/web/vendor/

# Frontend (npm / Vite) – tooling lives in repo root, not in web/ (not uploaded to hosting)
/node_modules/

# Nette
/web/temp/
/web/log/

# Local config overrides (secrets)
/web/app/config/local.neon
